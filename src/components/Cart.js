import React, { Component } from 'react';
import { connect } from 'react-redux';
import { onLogout, keepLogin, cookieChecked, selectPage } from '../actions';

class Cart extends Component {
    
}

export default Cart;